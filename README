To build:

make

To generate keys:
cd keygen && make && ./genkeys

To run:
./slitheen [interface to client] [interface to world]

To read from file for tests,
./slitheen [interface to client] -test [test_packets.pcap]
