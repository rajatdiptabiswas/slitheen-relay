To build:

make

To generate keys:
cd keygen && make && ./genkeys

To run:
./slitheen [interface to client] [interface to world]

To read packets from file for test,
./slitheen -test [pcap readfile] [pcap writefile]
